# 디자인 품질 판정 기준 (quality bar)

design이 산출한 화면·등록 자산이 "괜찮은가"를 **매번 같은 축으로** 판정하기
위한 기준이다. 기준이 없으면 품질은 매 세션 모델의 취향으로 흔들리고, 개선했는지
여부도 잴 수 없다.

## 이 기준의 지위 (정본 구분)

- 이 파일은 **harness가 정의한 판정 기준**이지 앱인토스 공식 디자인 가이드가
  아니다. 공식 가이드의 정본은 공식 문서이며, 조회는 docs MCP로 한다
  (`references/docs-lookup.md`).
- **1층 하드 규칙의 정본은 `render-rules.md`다.** 이 파일의 `차단` 등급 항목은
  그 1층을 화면 단위 판정 항목으로 옮겨 적은 것이다 — 문면이 어긋나면
  `render-rules.md` 쪽이 정본이고, 고치는 방법도 그 파일에 있다.
- 각 항목의 `근거` 열이 출처를 구분한다:
  - **H** — harness 기준(이 파일이 정의). 공식 문서를 대신 주장하지 않는다.
  - **D** — 공식 문서에서 확인해야 하는 항목. 확인 전에는 "공식 규격"이라고
    단정하지 말고 `(문서 확인 필요)`로 남긴다.
  - **W** — 공개 웹 표준(WCAG 등). 출처를 밝히고 인용한다.
- 항목을 추가·수정할 때 근거 없는 수치를 새로 만들지 않는다. 수치가 필요하면
  D로 두고 docs MCP로 확인하거나, 프로젝트가 스스로 정한 값(토큰)으로 돌린다.

## 판정 단위와 등급

- **판정 단위는 화면**이다. 화면 목록이 없으면 먼저 화면 목록부터 확정한다.
- 각 항목의 판정은 셋 중 하나: **통과** / **조정 필요**(사유 1줄 필수) /
  **해당 없음**(사유 1줄 필수).
- 각 항목에는 `등급` 열이 붙어 있다. **`차단`은 조정 필요가 남으면 완료가
  아니고 design이 직접 고쳐야 하는 항목**, **`권장`은 목록으로 남기고 진행할
  수 있는 항목**이다. 등급은 그룹이 아니라 항목에 붙는다 — 한 그룹 안에 두
  등급이 섞인다.
- "아마 괜찮다"로 통과시키지 않는다. 확인할 수단이 없으면 조정 필요가 아니라
  `(확인 필요)`로 적고 사용자에게 무엇을 확인해야 하는지 넘긴다.

## G0 — 브랜드·IP 안전

SKILL.md의 "토스 브랜드·UI 모방 금지" 절이 정의하는 금지 목록을 그대로 판정
항목으로 쓴다. **하나라도 걸리면 나머지 항목을 채점하지 않고 멈춘다**(그 절의
"위반 의심 시 절차"). G0은 유일하게 자동 수정 대상이 아니다 — 대안 선택이
사용자의 것이라 고치지 말고 확인받는다.

G0은 **두 번째** 관문이다 — 같은 목록의 첫 번째 적용은 SKILL.md 실행 순서
0단계(브랜드 체크포인트)이고, 그것은 자산·코드를 만들기 **전에** 통과해야
한다. 여기서 처음 걸린다는 것은 이미 만들어 놓고 채점하고 있다는 뜻이므로,
0단계가 건너뛰어졌다는 사실도 함께 보고한다.

| # | 항목 | 등급 | 근거 |
|---|---|---|---|
| G0-1 | 토스 로고·워드마크(변형 포함)를 아이콘·화면·등록 자산에 쓰지 않았다 | 차단 | H |
| G0-2 | 토스 브랜드 컬러를 미니앱 primary 색으로 채택하지 않았다 | 차단 | H |
| G0-3 | 토스 앱 화면(특히 로그인/인증)의 구성을 복제하지 않았다 | 차단 | H |
| G0-4 | "토스" 상호로 미니앱 자체를 토스 공식 제품처럼 표방하지 않았다 | 차단 | H |
| G0-5 | 토스 전용 본문 서체(`Toss Product Sans` 계열)를 `font-family`·웹폰트·번들 파일 어디로도 쓰지 않았다 — 시스템 폰트 스택이나 라이선스가 명확한 공개 웹폰트를 쓴다 | 차단 | H |
| G0-6 | 화면에 넣은 아이콘·이미지의 출처와 사용 조건을 확인했다 — 제작자 자신의 세트, 오픈 라이선스, 또는 앱인토스가 제공하는 아이콘 라이브러리다. 라이선스가 열려 있는지 모르는 자산(토스 내부 아이콘 포함)은 링크·번들하지 않는다 | 차단 | H |

이모지 서체 `Tossface`는 **G0-5에 걸리지 않는다** — 공개 배포되고 자체
라이선스가 붙어 있어 사용이 허용된다(SKILL.md의 대안 절). 다만 폰트 파일을
직접 번들·재배포하는 경우 라이선스가 저작권 안내 동봉을 요구하므로, 그 경로를
골랐으면 안내가 함께 남아 있는지 본다.

## G1 — 컨테이너 적합성

미니앱이 토스 앱 WebView 컨테이너 안에서 도는 데서 오는 제약 축이다.

| # | 항목 | 등급 | 근거 |
|---|---|---|---|
| G1-1 | 최상단 콘텐츠가 safe-area inset을 반영한다(고정 padding 가정 아님) | 차단 | H |
| G1-2 | 좌측 엣지에 가로 드래그 UI를 겹쳐 swipe-back과 충돌시키지 않는다 | 차단 | H |
| G1-3 | 상단 네비게이션 바를 화면 안에 직접 그리지 않는다 — 컨테이너가 자동으로 붙이므로 직접 그리면 두 겹이 된다. 제목·우측 액션이 필요하면 플랫폼 네비로 넣는다 | 차단 | H |
| G1-4 | 화면 전환·뒤로가기 거동이 공식 문서의 네비게이션 안내와 어긋나지 않는다 | 차단 | D |
| G1-5 | 웹(React DOM) 레이아웃으로 설계됐다 — RN 네이티브 컴포넌트를 전제하지 않는다 | 차단 | H |
| G1-6 | 화면 하단에 고정되는 CTA·바가 화면 맨 밑에 붙지 않는다 — 하단 safe-area만큼(별도 근거가 없으면 통용 하한인 34px) 여백을 둔다 | 차단 | H |

## G2 — 등록 자산 규격

SKILL.md 5·6단계가 이미 강제하는 축이다. 여기서는 품질 판정에도 같은 결과를
그대로 인용한다 — **자산 규격은 눈으로 보지 말고 실제 픽셀 치수로 판정**한다.

| # | 항목 | 등급 | 근거 |
|---|---|---|---|
| G2-1 | 필수 자산(logo·thumbnail·세로 스크린샷 ≥3)이 모두 존재한다 | 차단 | H |
| G2-2 | 각 파일의 실측 픽셀 치수가 규격표와 일치한다(6단계 검증 통과) | 차단 | H |
| G2-3 | 종횡비를 늘려 맞춘 자산이 없다(크롭/패딩 의도는 사용자 확인을 거쳤다) | 차단 | H |
| G2-4 | 플레이스홀더가 섞여 있으면 완료 안내에 그 사실이 명시돼 있다 | 차단 | H |
| G2-5 | 규격표가 현재 콘솔 등록 tool의 실제 입력 스키마와 어긋나지 않는다 | 차단 | D |

## G3 — 토큰 일관성

| # | 항목 | 등급 | 근거 |
|---|---|---|---|
| G3-1 | 색·타이포·spacing이 이름 있는 토큰으로 정의돼 있다(값이 한 곳에 모여 있다) | 권장 | H |
| G3-2 | 같은 역할의 값이 화면마다 다르지 않다(예: 본문 텍스트 색이 화면별로 제각각이 아님) | 권장 | H |
| G3-3 | 화면 코드에 토큰 밖 하드코딩 hex·px가 남아 있지 않다(남았다면 목록으로 짚는다) | 권장 | H |
| G3-4 | 다크 모드를 지원한다면 토큰이 모드별로 쌍을 이룬다(지원 안 하면 "해당 없음") | 권장 | H |
| G3-5 | 화면의 모든 텍스트가 타입 스케일의 최소·최대 범위 안에 있다 — 11px 이하가 없고 본문은 15px 이상이다(13px대는 메타데이터 전용) | 차단 | H |
| G3-6 | `font-weight`가 400~700 범위 안에 있다(800·900은 브라우저 합성 볼드로 글자가 뭉개진다) | 차단 | H |
| G3-7 | 한 문장·한 라벨 안에서 글자 크기가 튀지 않는다 — 강조는 크기가 아니라 weight·색으로 준다 | 차단 | H |
| G3-8 | 모든 이모지가 Tossface로 렌더된다(진입 CSS 폰트 스택 맨 앞에 배선했거나 개별 요소에 `.tf`) — 한 화면에서 어떤 이모지만 시스템 폰트로 빠지지 않는다. `--no-tossface` 프로젝트에서는 "해당 없음" | 차단 | H |

## G4 — 화면 구조·조작성

| # | 항목 | 등급 | 근거 |
|---|---|---|---|
| G4-1 | 화면마다 주요 액션이 하나로 읽힌다 — 동급 CTA가 나열돼 우선순위가 사라지지 않았고, 그 액션이 칩·캡션 크기로 작게 그려져 있지도 않다 | 차단 | H |
| G4-2 | 주요 액션이 한 손 조작 범위 안에 있다(화면 하단 접근 가능 영역) | 권장 | H |
| G4-3 | 인터랙티브 요소의 터치 타깃이 손가락 조작에 충분하다 — 프로젝트가 정한 하한 토큰 기준, 별도 근거가 없으면 통용되는 하한인 44×44 CSS px 이상 | 차단 | W (모바일 접근성 통념) |
| G4-4 | 본문 텍스트와 배경의 명도 대비가 WCAG 2.1 AA(일반 텍스트 4.5:1, 큰 텍스트 3:1) 이상이다 | 차단 | W |
| G4-5 | 주요 액션이 콘텐츠에 밀려 도달 불가해지지 않는다 — 스크롤 화면에서 CTA가 가려지지 않고, 과대한 이미지·히어로가 액션을 접힘선 아래로 밀어내지도 않는다 | 차단 | H |
| G4-6 | 화면의 **모든** 텍스트 크기를 전수로 훑어 판정했다 — 눈대중으로 통과시키지 않는다(수치 기준은 G3-5) | 차단 | H (모바일 가독성 통념) |
| G4-7 | 버튼이 버튼으로 읽힌다 — 배경과 구분되는 배경색·테두리·그림자 중 최소 하나를 갖는다 | 차단 | H |

## G5 — 상태 완전성

화면을 "행복 경로"만 그리는 것이 품질 저하의 가장 흔한 형태다. 데이터·권한이
관여하는 화면은 아래 4상태가 **설계상 정의돼 있어야** 한다(구현 여부가 아니라
설계에 존재하는지를 본다).

| # | 항목 | 등급 | 근거 |
|---|---|---|---|
| G5-1 | 로딩 상태가 정의돼 있다 | 권장 | H |
| G5-2 | 빈 상태(결과 0건·최초 진입)가 정의돼 있다 | 권장 | H |
| G5-3 | 실패 상태가 정의돼 있고 재시도 경로가 있다 | 권장 | H |
| G5-4 | 권한이 필요한 기능은 거부·보류 상태의 화면이 정의돼 있다 | 권장 | H |

## G6 — 카피

전 항목이 권장이다. 조정 필요가 나오면 design이 문구를 다시 쓰지 않고
`/ait:ux-writing`으로 넘긴다 — 재작성이 끝나면 이 그룹만 재판정한다.

| # | 항목 | 등급 | 근거 |
|---|---|---|---|
| G6-1 | 사용자 문구의 어투가 화면 전반에서 일관된다 | 권장 | H |
| G6-2 | 에러 문구가 원인과 다음 행동을 함께 말한다(단순 "오류가 발생했습니다" 금지) | 권장 | H |
| G6-3 | 과장·홍보성 표현, 근거 없는 수치·수상 문구가 없다 | 권장 | H |
| G6-4 | 한 화면 안에서 언어를 섞지 않는다 | 권장 | H |
| G6-5 | 카테고리·검수 관련 문구가 공식 검수 기준과 어긋나지 않는다 | 권장 | D |
| G6-6 | 손실·불안을 자극하는 프레이밍("놓치면 손해"류)이 없다 — 같은 내용은 사용자가 얻는 것으로 말한다 | 권장 | H |
| G6-7 | 사용자의 감정을 대신 단정하는 문구("안심돼요"·"기분 좋은")가 없다 — 확인 가능한 사실로 말한다 | 권장 | H |
| G6-8 | 한 문장에 조건·수치가 3개 이상 나열되지 않는다 — 넘치면 문장을 나눈다 | 권장 | H |
| G6-9 | 같은 주장을 한 화면 안에서 표현만 바꿔 반복하지 않는다(상단 라벨·제목·부제가 같은 말을 겹쳐 하는 형태) | 권장 | H |

## G7 — 렌더 무결성·시각 안티패턴

생성된 화면이 실제 렌더에서 깨지거나 검토 없이 만들어진 티를 내는 시각 결함
축이다. 심미성 점수화가 아니라 결함 유무 판정이다("이 기준이 하지 않는 것"의
❌ 심미성 점수화와 충돌하지 않는다). 각 항목은 코드에서 재현 가능하게 확인한다.

| # | 항목 | 등급 | 근거 |
|---|---|---|---|
| G7-1 | 텍스트·요소가 서로 겹치지 않고 화면 밖으로 잘리지 않는다(음수 마진·고정 높이 넘침을 확인한다) | 차단 | H |
| G7-2 | 화면 안 이미지가 원본 종횡비를 유지한다 — 늘리거나 눌러 맞추지 않는다(등록 자산의 같은 규칙은 G2-3) | 차단 | H |
| G7-3 | 방향·행동 아이콘(chevron·화살표 등)을 텍스트 글리프(`>`·`»`)로 때우지 않는다 — 인라인 SVG로 넣고 색은 `currentColor`로 잇는다 | 차단 | H |
| G7-4 | 카드·버튼처럼 네 변이 동일한 형태인 요소에 한쪽 변만 다른 색으로 칠한 보더를 쓰지 않는다 — 둥근 모서리에서는 편측 보더가 곡률과 이어지지 않아 렌더가 어긋나 보인다. 강조는 균일한 사방 테두리·배경색 변화·별도 배지처럼 카드 전체에 고르게 적용되는 방식으로 표현한다 | 권장 | H |
| G7-5 | 상시 요소(카드·버튼·입력·리스트 행)에 드롭 섀도가 있으면 목록으로 짚는다 — 뜬 요소(다이얼로그·시트·툴팁·드롭다운)와 달리 띄움 위계가 없는 곳의 섀도는 계층 관계를 흐리게 만든다 | 권장 | H |
| G7-6 | 한국어 텍스트 블록에 `word-break: keep-all`이 적용돼 어절 중간에서 줄이 바뀌지 않는다 | 차단 | H |
| G7-7 | 화면 배경색이 뷰포트 전체를 채운다 — 콘텐츠 높이가 짧아 아래쪽이 빈 흰 여백으로 남지 않는다 | 차단 | H |
| G7-8 | 의도 없는 여백·정렬 어긋남이 없다 — 우측에 빈 여백이 생기거나, 특정 요소만 이유 없이 들여쓰기되지 않는다 | 차단 | H |
| G7-9 | 버튼 안에 방향 화살표·꺾쇠를 넣지 않는다 — 버튼은 액션이지 이동 표시가 아니다(화살표는 링크·리스트 행의 이동 어포던스에만 쓴다) | 차단 | H |
| G7-10 | `render-rules.md` 2층(권장) 위반을 목록으로 남겼다 — 2층은 항목으로 채점하지 않고 걸린 것만 사용자에게 보인다 | 권장 | H |

## G8 — 다크패턴·광고

사용자를 속이거나 가두는 상호작용 패턴 축이다. 앞 축들과 달리 "품질이
낮다"가 아니라 "사용자에게 해롭다"에 해당하므로 전 항목이 차단이다. 광고가
없는 앱은 광고 항목을 해당 없음으로 판정한다.

| # | 항목 | 등급 | 근거 |
|---|---|---|---|
| G8-1 | 진입 직후 첫 화면이 전면 광고·팝업·동의 시트로 가로막히지 않는다 | 차단 | H |
| G8-2 | 뒤로가기·닫기가 함정이 아니다 — 닫기 없는 시트/전면 요소, 나가기를 막는 확인 반복, 막다른 화면이 없다 | 차단 | H |
| G8-3 | 가짜 버튼·위장 광고가 없다 — 광고·유도 요소는 콘텐츠와 시각적으로 구분되고, 닫기를 숨기거나 극단적으로 작게 만들지 않는다 | 차단 | H |
| G8-4 | 고정 배너·플로팅 요소가 CTA·내비게이션을 가리거나 오터치를 유도하는 위치에 있지 않다 | 차단 | H |
| G8-5 | 광고를 싣는 방식이 공식 문서의 광고·검수 안내와 어긋나지 않는다 | 차단 | D |
| G8-6 | 광고가 콘텐츠를 가리거나 첫 화면(ATF)의 핵심 콘텐츠·액션을 접힘 아래로 밀어내지 않는다 | 차단 | H |
| G8-7 | 하단 탭바가 있는 화면에 광고 배너를 바닥 고정으로 두지 않는다 — 탭바가 있으면 광고는 콘텐츠 중간에 넣는다 | 차단 | H |
| G8-8 | 같은 동작을 하는 CTA를 한 화면에 중복해서 두지 않는다 | 차단 | H |

## 완료 판정 규칙

1. **G0에 조정 필요가 1건이라도 있으면 즉시 중단** — 채점을 이어가지 않고
   사용자에게 해당 항목을 알린 뒤 확인을 받는다. G0은 고쳐서 넘기지 않는다.
2. **`차단` 등급 항목에 조정 필요가 남아 있으면 design은 "완료"가 아니다** —
   어느 화면의 어느 항목인지 짚고, 목록으로 넘기지 말고 렌더 단계로 돌아가
   고친 뒤 같은 항목을 재판정한다(G0은 규칙 1을 따른다).
   차단 항목을 가진 그룹: G0·G1·G2·G3·G4·G7·G8.
3. **`권장` 등급은 조정 필요를 목록으로 남기고 진행할 수 있다.** 단 조정
   필요가 **화면당 3건 이상**이면 완료로 선언하지 않고 사용자에게 우선순위를
   묻는다.
4. **수정 루프는 같은 항목에 대해 최대 2회**까지 돈다. 2회로 안 풀리면 계속
   고치지 말고 무엇이 걸렸는지 적어 넘기고, 그 항목을 `(확인 필요)`로 남긴다.
5. `(확인 필요)`(판정 수단 없음) 항목은 조정 필요와 별도로 세어 완료 안내에
   그대로 노출한다 — 조용히 통과시키지 않는다.
6. D 근거 항목을 공식 규격으로 단정하려면 docs MCP 조회 결과가 있어야 한다.
   조회하지 않았으면 `(문서 확인 필요)`로 남긴다.

## 자기 점검 출력 형식

채점 결과는 화면별 표 + 요약 한 줄로 인쇄한다. 형식을 고정해야 세션 간 비교가
가능하다.

```
품질 점검 (quality bar)

  홈
    G1 컨테이너   통과
    G3 토큰       조정 필요 — 카드 배경이 #F5F5F5 하드코딩, surface 토큰 없음 (권장)
    G4 구조·조작  통과
    G5 상태       조정 필요 — 목록 0건일 때 빈 상태 미정의 (권장)
    G6 카피       통과
    G7 렌더       통과
    G8 다크패턴   해당 없음 — 광고·전면 요소 없음
  상세
    G1 컨테이너   조정 필요 — 커스텀 헤더가 컨테이너 헤더와 이중 (차단)
    G3 토큰       통과
    G4 구조·조작  확인 필요 — 대비비를 잴 색상값 미확정
    G5 상태       통과
    G6 카피       통과
    G7 렌더       통과
    G8 다크패턴   해당 없음 — 광고·전면 요소 없음

  자산 (G2)       통과 — 필수 5종 규격 일치
  브랜드 (G0)     통과

  요약: 차단 1건 / 권장 2건 / 확인 필요 1건 — G1-3 미해소가 있어 완료 보류
```

차단 항목을 고쳤으면 같은 표를 다시 인쇄해 해소됐음을 보인다. 자동 수정한
항목은 무엇을 어떻게 바꿨는지 한 줄로 함께 적는다.

## 이 기준이 하지 않는 것

- ❌ 심미성 점수화 — "예쁨"을 숫자로 매기지 않는다. 이 기준은 **재현 가능한
  항목**만 다룬다.
- ❌ 공식 디자인 가이드 대체 — D 근거 항목은 문서가 정본이고 이 파일은 그것을
  조회하도록 가리킬 뿐이다.
- ❌ 3층 판정 — 브랜드색·폰트·톤·레이아웃 취향은 제작자의 것이라 항목으로
  올리지 않는다(`render-rules.md` 3층).

**판정에서 멈추는 것도 여기 없다.** `차단` 등급에 조정 필요가 남으면 design은
목록만 넘기지 않고 렌더 단계로 돌아가 화면 코드를 직접 고친 뒤 재판정한다
(SKILL.md 4단계). 이 파일이 정의하는 것은 *무엇을* 고쳐야 하는가이고, *어떻게*
고치는가는 `render-rules.md`다.
