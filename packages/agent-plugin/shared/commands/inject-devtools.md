---
description: 'Add devtools unplugin to an existing project.'
argument-hint: ''
---

Load the `inject` skill — the user invoked the **devtools facet** (`/ait:inject-devtools`): they want to add the `@apps-in-toss/devtools` unplugin to an existing project's build config for browser dev. Jump to the "devtools facet — `/ait:inject-devtools`" section, not the debug-console facet. Takes no argument.
