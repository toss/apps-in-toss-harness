---
description: 'Install @apps-in-toss/debug-console into an existing project as a dependency — setup only, not a diagnosis (use /ait:debug to debug a running app).'
argument-hint: ''
---

Load the `inject` skill — the user invoked the **debug-console facet** (`/ait:inject-debug-console`): they want to install `@apps-in-toss/debug-console` (on-device attach + eruda, the only debug package that ships in the production bundle) as a `dependencies` entry and wire up the `/auto` self-gating import. Jump to the "debug-console facet — `/ait:inject-debug-console`" section, not the devtools facet. Takes no argument.
