---
description: 'Migrate an existing project to polyfill mode — install @ait-co/polyfill and wire up the entry point.'
argument-hint: '[--entry <path>]'
---

Load the `inject` skill — the user invoked the **polyfill facet** (`/ait:inject-polyfill`): they want to migrate an existing project to `@ait-co/polyfill` mode (standard Web API calls routing to the SDK). Jump to the "polyfill facet — `/ait:inject-polyfill`" section, not the devtools facet. The optional `[--entry <path>]` argument names the entry point.
