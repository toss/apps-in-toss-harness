---
description: Turn natural-language mini-app requirements into a plan — the SDK domains, runtime permissions, and console terms (약관) you'll need before scaffolding.
argument-hint: '[requirements]'
---

Load the `plan` skill and analyze the requirements in `$ARGUMENTS`.
