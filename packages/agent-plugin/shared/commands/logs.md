---
description: 'Fetch recent runtime logs from the console (alternative guidance — aitcc logs endpoint pending).'
argument-hint: ''
---

Load the `status` skill — the user invoked the **logs facet** (`/ait:logs`): they want runtime logs for the current mini-app. Jump to the "logs facet — `/ait:logs`" section (the confirmed console runtime-log gap + the four practical alternatives), not the console status query.
