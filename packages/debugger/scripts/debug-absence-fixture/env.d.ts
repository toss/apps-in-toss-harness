// Consumer-build constant injected by this fixture's vite.config.ts `define`
// (devtools#647). Fixture-scoped: this package's `src/` must never reference
// __DEBUG_BUILD__ bare, so it is deliberately absent from src/env.d.ts.
declare const __DEBUG_BUILD__: boolean;
