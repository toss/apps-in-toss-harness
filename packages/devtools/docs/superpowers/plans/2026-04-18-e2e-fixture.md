# E2E Fixture Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** devtools의 Playwright E2E를 자기완결(fixture 기반)로 만든다. sdk-example git clone 경로를 제거하고, `e2e/fixture/` 에 Vite + `@ait-co/devtools/unplugin`로 돌아가는 최소 consumer 앱을 두고 `e2e/panel.test.ts`를 21개(Smoke 1 + Layer A 12 + Layer B 4 + Layer C 4)로 재작성한다. `pnpm test:e2e` 로컬 2회 연속 green이 완료 조건.

**Architecture:** fixture는 flat HTML + `main.ts` + `helpers.ts` + `vite.config.ts`. Playwright webServer가 devtools를 빌드한 뒤 fixture vite build → vite preview로 port 4173에 띄운다. 테스트는 helper가 생성한 testid 규약(`<id>-btn`, `<id>-result`, `<id>-input`, `<id>-value`, `<id>-log`, `section-<id>`)으로 DOM을 찾는다. panel은 unplugin의 `panel: true` 옵션이 entry에 자동 주입.

**Tech Stack:** TypeScript 6, Vite 8 (rolldown; Vite 6 계획 중 최신으로 교체), Playwright 1.59, pnpm 10.33.0, Node 24. devtools 기존 tsdown / tsc / vitest는 그대로. Vite 8 + rolldown 이슈로 unplugin resolveId/transform 대신 resolve.alias + 명시적 panel import를 사용함 (vite.config.ts 주석 참고).

**Base branch:** `main`. 현재 작업 브랜치 `test-e2e-selector-audit` 에 이어서 커밋한다 (spec/plan 문서 3개가 이미 이 브랜치에 있음).

**Spec:** `docs/superpowers/specs/2026-04-18-e2e-fixture-design.md`

**대체/삭제되는 문서 (Task 11에서 일괄 정리):**
- `docs/superpowers/specs/2026-04-18-e2e-testid-contract-design.md`
- `docs/superpowers/plans/2026-04-18-A-e2e-selector-audit.md`
- `docs/superpowers/plans/2026-04-18-e2e-testid-contract.md`

---

## File Structure

### 신규

| Path | 책임 |
|---|---|
| `e2e/fixture/index.html` | `<div id="app">` + entry module 태그 |
| `e2e/fixture/main.ts` | 도메인 섹션 16개 렌더. SDK API 실행 버튼/입력/값 표시. unplugin이 panel 자동 주입 |
| `e2e/fixture/helpers.ts` | `apiSection / apiButton / apiInput / apiValue / apiSubscriber` DOM helper. testid 규약 생성 |
| `e2e/fixture/vite.config.ts` | `aitDevtools.vite({ panel: true })` 적용 |
| `e2e/fixture/tsconfig.json` | 루트 tsconfig를 extends, `include: ["**/*.ts"]`, `lib: ["ES2022", "DOM", "DOM.Iterable"]` |

### 수정

| Path | 변경 |
|---|---|
| `playwright.config.ts` | `webServer.command`를 fixture 빌드/preview로 교체. clone/install 스텝 제거 |
| `e2e/panel.test.ts` | 전면 재작성 (848줄 → ~420줄 예상, 21 테스트) |
| `package.json` | devDependencies에 `vite ^6`, `@types/node` 필요 시 추가 (Node env 사용 안 함이면 스킵) |
| `.gitignore` | `e2e/fixture/dist/`, `e2e/fixture/node_modules/`(없지만 방어), 기존 `.tmp/sdk-example` 항목은 더 이상 생성되지 않지만 남겨두어 무방 |

### 삭제

| Path | 이유 |
|---|---|
| `docs/superpowers/specs/2026-04-18-e2e-testid-contract-design.md` | 현 spec이 대체 |
| `docs/superpowers/plans/2026-04-18-A-e2e-selector-audit.md` | 폐기 |
| `docs/superpowers/plans/2026-04-18-e2e-testid-contract.md` | 폐기 |

---

## Task 1: Vite devDependency 추가 + 골조 디렉토리 만들기

**Files:**
- Modify: `package.json`
- Create: `e2e/fixture/` 디렉토리 (Task 2에서 실제 파일 생성)

- [ ] **Step 1: Vite 추가**

Run:
```bash
cd /Users/dave/Projects/github.com/apps-in-toss-community/devtools-test-e2e-selector-audit
pnpm add -D vite
```

Expected: `package.json` devDependencies에 `"vite": "^6.x.x"` 추가 (current latest 6.x). `pnpm-lock.yaml` 갱신.

- [ ] **Step 2: 디렉토리 확인 + 초기 커밋**

Run:
```bash
mkdir -p e2e/fixture
git add package.json pnpm-lock.yaml
git commit -m "chore(e2e): add vite devDependency for fixture app"
```

Expected: 커밋 성공. `e2e/fixture/`는 빈 디렉토리라 커밋 대상 아님 (Task 2에서 파일 추가 시 함께 추가됨).

---

## Task 2: Fixture vite.config.ts + tsconfig.json + index.html

**Files:**
- Create: `e2e/fixture/vite.config.ts`
- Create: `e2e/fixture/tsconfig.json`
- Create: `e2e/fixture/index.html`

- [ ] **Step 1: `e2e/fixture/vite.config.ts`**

```ts
import { defineConfig } from 'vite';
import aitDevtools from '@ait-co/devtools/unplugin';

// Fixture app that hosts the devtools panel + mock and exercises a
// representative subset of SDK APIs, used only by Playwright E2E.
// `aitDevtools.vite({ panel: true })` aliases `@apps-in-toss/web-framework`
// to the mock and auto-injects `@ait-co/devtools/panel` into the entry.
export default defineConfig({
  root: __dirname,
  plugins: [aitDevtools.vite({ panel: true })],
  preview: {
    port: 4173,
    strictPort: true,
  },
  build: {
    outDir: 'dist',
    emptyOutDir: true,
  },
});
```

- [ ] **Step 2: `e2e/fixture/tsconfig.json`**

```json
{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "rootDir": ".",
    "outDir": "./dist",
    "types": [],
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "noEmit": true
  },
  "include": ["**/*.ts"],
  "exclude": ["dist", "node_modules"]
}
```

`types: []`는 루트 tsconfig의 `vitest/globals`를 fixture에선 제외하기 위함.

- [ ] **Step 3: `e2e/fixture/index.html`**

```html
<!doctype html>
<html lang="ko">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>devtools E2E fixture</title>
    <style>
      body { font-family: -apple-system, BlinkMacSystemFont, sans-serif; margin: 0; padding: 16px; max-width: 640px; }
      section { border-top: 1px solid #eee; padding: 12px 0; }
      h2 { font-size: 14px; margin: 0 0 8px; }
      button { display: inline-block; margin: 2px 4px 2px 0; padding: 4px 8px; font-size: 12px; }
      .row { display: flex; gap: 8px; align-items: center; margin-bottom: 4px; }
      .result { font-family: ui-monospace, monospace; font-size: 11px; color: #333; word-break: break-all; min-height: 14px; }
      .value { font-family: ui-monospace, monospace; font-size: 12px; color: #111; }
      label { font-size: 12px; color: #555; }
      input { font-size: 12px; padding: 2px 4px; }
    </style>
  </head>
  <body>
    <h1>devtools E2E fixture</h1>
    <div id="app"></div>
    <script type="module" src="./main.ts"></script>
  </body>
</html>
```

- [ ] **Step 4: 커밋**

Run:
```bash
git add e2e/fixture/vite.config.ts e2e/fixture/tsconfig.json e2e/fixture/index.html
git commit -m "feat(e2e): scaffold fixture vite config, tsconfig, and HTML shell"
```

---

## Task 3: Fixture helpers (`e2e/fixture/helpers.ts`)

**Files:**
- Create: `e2e/fixture/helpers.ts`

Helper 5종이 testid 규약을 코드로 강제한다. 세밀 id 명명은 Task 5에서 호출 시 확정.

- [ ] **Step 1: `e2e/fixture/helpers.ts` 생성**

```ts
// DOM helpers that enforce the testid convention used by e2e/panel.test.ts.
//
// testid 규약:
//   section-<id>   — 도메인 섹션 <section> 루트
//   <id>-btn       — API 실행 버튼
//   <id>-result    — 실행 결과 표시 영역 (.textContent)
//   <id>-input     — 사용자 입력 필드
//   <id>-value     — 페이지 로드 시 즉시 표시되는 읽기 전용 값
//   <id>-log       — 구독형 이벤트 수신 로그 컨테이너
//   <id>-empty     — 구독형 로그의 empty state sentinel
//
// id는 dash-case 권장. 동일 id가 여러 도메인에서 재사용되면 안 된다.

export function apiSection(parent: HTMLElement, id: string, title: string): HTMLElement {
  const section = document.createElement('section');
  section.setAttribute('data-testid', `section-${id}`);
  const heading = document.createElement('h2');
  heading.textContent = title;
  section.appendChild(heading);
  parent.appendChild(section);
  return section;
}

export interface ApiButtonOptions<T> {
  label?: string; // Button text (defaults to id)
  formatResult?: (value: T) => string; // Custom result formatter
  withInputs?: string[]; // Input testids to read into an object and pass to run(values)
}

/**
 * Renders a button + result pair. Clicking the button calls run() and writes
 * the outcome to the result div:
 *   - Success with primitive value: String(value)
 *   - Success with object: JSON.stringify(value)
 *   - Success with undefined: 'done'
 *   - Error: 'error:' + err.message (or String(err))
 *
 * Result is always assigned synchronously after await, so Playwright's
 * expect(locator).toHaveText(...) converges without extra waits.
 */
export function apiButton<T = unknown>(
  parent: HTMLElement,
  id: string,
  run: (values: Record<string, string>) => Promise<T> | T,
  opts: ApiButtonOptions<T> = {},
): void {
  const row = document.createElement('div');
  row.className = 'row';

  const button = document.createElement('button');
  button.type = 'button';
  button.setAttribute('data-testid', `${id}-btn`);
  button.textContent = opts.label ?? id;
  row.appendChild(button);

  const result = document.createElement('div');
  result.className = 'result';
  result.setAttribute('data-testid', `${id}-result`);
  row.appendChild(result);

  parent.appendChild(row);

  button.addEventListener('click', async () => {
    const values: Record<string, string> = {};
    for (const inputId of opts.withInputs ?? []) {
      const el = document.querySelector<HTMLInputElement>(`[data-testid="${inputId}-input"]`);
      values[inputId] = el?.value ?? '';
    }
    try {
      const value = await run(values);
      if (opts.formatResult) {
        result.textContent = opts.formatResult(value);
      } else if (value === undefined) {
        result.textContent = 'done';
      } else if (typeof value === 'object') {
        result.textContent = JSON.stringify(value);
      } else {
        result.textContent = String(value);
      }
    } catch (err) {
      const msg = err instanceof Error ? err.message : String(err);
      result.textContent = `error:${msg}`;
    }
  });
}

export function apiInput(parent: HTMLElement, id: string, label: string): void {
  const row = document.createElement('div');
  row.className = 'row';

  const lbl = document.createElement('label');
  lbl.textContent = label;
  row.appendChild(lbl);

  const input = document.createElement('input');
  input.type = 'text';
  input.setAttribute('data-testid', `${id}-input`);
  row.appendChild(input);

  parent.appendChild(row);
}

export function apiValue(parent: HTMLElement, id: string, label: string): HTMLElement {
  const row = document.createElement('div');
  row.className = 'row';

  const lbl = document.createElement('label');
  lbl.textContent = label;
  row.appendChild(lbl);

  const value = document.createElement('span');
  value.className = 'value';
  value.setAttribute('data-testid', `${id}-value`);
  row.appendChild(value);

  parent.appendChild(row);
  return value;
}

export interface ApiSubscriberOptions {
  label?: string;
}

/**
 * Renders a subscriber pair: an "activate" button and a log area. The log
 * starts with an `<id>-empty` placeholder; each event received appends a
 * `<div>` child to the `<id>-log` container.
 */
export function apiSubscriber(
  parent: HTMLElement,
  id: string,
  subscribe: (onEvent: (payload: unknown) => void) => void,
  opts: ApiSubscriberOptions = {},
): void {
  const row = document.createElement('div');
  row.className = 'row';

  const button = document.createElement('button');
  button.type = 'button';
  button.setAttribute('data-testid', `${id}-btn`);
  button.textContent = opts.label ?? `subscribe ${id}`;
  row.appendChild(button);

  const empty = document.createElement('span');
  empty.setAttribute('data-testid', `${id}-empty`);
  empty.textContent = '(no events)';
  row.appendChild(empty);

  const log = document.createElement('div');
  log.setAttribute('data-testid', `${id}-log`);
  row.appendChild(log);

  parent.appendChild(row);

  button.addEventListener(
    'click',
    () => {
      subscribe((payload) => {
        empty.remove();
        const entry = document.createElement('div');
        entry.textContent = payload === undefined ? '(event)' : JSON.stringify(payload);
        log.appendChild(entry);
      });
      button.disabled = true;
    },
    { once: true },
  );
}
```

- [ ] **Step 2: 커밋**

Run:
```bash
git add e2e/fixture/helpers.ts
git commit -m "feat(e2e): add fixture DOM helpers with testid convention"
```

---

## Task 4: Playwright webServer 교체

**Files:**
- Modify: `playwright.config.ts`

- [ ] **Step 1: 현재 내용 확인**

현재 파일:
```ts
import { defineConfig } from '@playwright/test';

export default defineConfig({
  testDir: './e2e',
  timeout: 30_000,
  fullyParallel: true,
  retries: process.env.CI ? 1 : 0,
  baseURL: 'http://localhost:4173',
  projects: [{ name: 'chromium', use: { browserName: 'chromium' } }],
  webServer: {
    command: [
      'pnpm build',
      'rm -rf .tmp/sdk-example',
      'git clone --depth 1 https://github.com/apps-in-toss-community/sdk-example.git .tmp/sdk-example',
      'cd .tmp/sdk-example && pnpm install && pnpm build && pnpm preview --port 4173',
    ].join(' && '),
    port: 4173,
    reuseExistingServer: !process.env.CI,
  },
});
```

- [ ] **Step 2: webServer 교체**

새 내용:
```ts
import { defineConfig } from '@playwright/test';

export default defineConfig({
  testDir: './e2e',
  testMatch: 'panel.test.ts',
  timeout: 30_000,
  fullyParallel: true,
  retries: process.env.CI ? 1 : 0,
  baseURL: 'http://localhost:4173',
  projects: [{ name: 'chromium', use: { browserName: 'chromium' } }],
  webServer: {
    command: [
      // Build devtools so @ait-co/devtools/{mock,panel,unplugin} dist files exist
      'pnpm build',
      // Build the self-contained fixture app (uses devtools unplugin to swap
      // @apps-in-toss/web-framework -> devtools mock, and auto-injects panel)
      'pnpm exec vite build --config e2e/fixture/vite.config.ts',
      // Serve fixture for Playwright
      'pnpm exec vite preview --config e2e/fixture/vite.config.ts --port 4173',
    ].join(' && '),
    port: 4173,
    reuseExistingServer: !process.env.CI,
  },
});
```

변경점:
- `testMatch: 'panel.test.ts'` — `e2e/fixture/**/*.ts`가 실수로 Playwright에 잡히지 않게 방어.
- webServer 명령을 devtools build → fixture build → fixture preview로 교체.
- sdk-example clone 로직 제거.

- [ ] **Step 3: 기존 `.tmp/sdk-example` 캐시 정리**

Run:
```bash
rm -rf .tmp/sdk-example
```

Expected: 디렉토리 삭제. `.gitignore`에 이미 `.tmp/` 포함되어 있어 git diff 영향 없음.

- [ ] **Step 4: 커밋**

Run:
```bash
git add playwright.config.ts
git commit -m "test(e2e): switch webServer from sdk-example clone to in-repo fixture"
```

---

## Task 5: Fixture `main.ts` — 16개 도메인 섹션

**Files:**
- Create: `e2e/fixture/main.ts`

이 Task는 크지만 완전히 선언적이다. 각 섹션이 1 helper 호출 덩어리라서 조각별로 쉽게 추가된다. 구조가 틀렸을 경우 한 번에 보기 쉽도록 한 파일에 모아 작성.

- [ ] **Step 1: `e2e/fixture/main.ts` 전체 내용 작성**

```ts
// E2E fixture consumer app for @ait-co/devtools.
//
// Imports @apps-in-toss/web-framework — at build time the devtools unplugin
// aliases that to @ait-co/devtools/mock, so every call below hits the mock.
// The unplugin also auto-injects `import '@ait-co/devtools/panel'` into this
// entry, so the floating panel appears without an explicit import.

import {
  // auth
  appLogin,
  getIsTossLoginIntegratedService,
  getUserKeyForGame,
  appsInTossSignTossCert,
  // navigation
  closeView,
  openURL,
  share,
  getTossShareLink,
  setIosSwipeGestureEnabled,
  setDeviceOrientation,
  setScreenAwakeMode,
  setSecureScreen,
  requestReview,
  // environment
  getPlatformOS,
  getOperationalEnvironment,
  getTossAppVersion,
  isMinVersionSupported,
  getSchemeUri,
  getLocale,
  getDeviceId,
  getGroupId,
  getNetworkStatus,
  getServerTime,
  SafeAreaInsets,
  // events
  graniteEvent,
  tdsEvent,
  onVisibilityChangedByTransparentServiceWeb,
  // permissions
  getPermission,
  openPermissionDialog,
  requestPermission,
  // device
  Storage,
  getCurrentLocation,
  openCamera,
  fetchAlbumPhotos,
  fetchContacts,
  getClipboardText,
  setClipboardText,
  generateHapticFeedback,
  saveBase64Data,
  // iap
  IAP,
  checkoutPayment,
  // ads
  GoogleAdMob,
  TossAds,
  loadFullScreenAd,
  showFullScreenAd,
  // game
  grantPromotionReward,
  grantPromotionRewardForGame,
  submitGameCenterLeaderBoardScore,
  getGameCenterGameProfile,
  openGameCenterLeaderboard,
  contactsViral,
  // analytics
  Analytics,
  eventLog,
  // partner
  partner,
} from '@apps-in-toss/web-framework';
import { apiButton, apiInput, apiSection, apiSubscriber, apiValue } from './helpers.js';

const app = document.getElementById('app');
if (!app) throw new Error('#app not found');

// --- Auth ---
{
  const s = apiSection(app, 'auth', 'Auth');
  apiButton(s, 'auth-login', async () => await appLogin());
  apiButton(s, 'auth-toss-integrated', () => getIsTossLoginIntegratedService());
  apiButton(s, 'auth-userkey', async () => await getUserKeyForGame());
  apiButton(s, 'auth-cert', async () => {
    await appsInTossSignTossCert({ txId: 'mock-tx' });
    return undefined;
  });
}

// --- Navigation ---
{
  const s = apiSection(app, 'navigation', 'Navigation');
  apiButton(s, 'nav-sharelink', async () => await getTossShareLink({ path: '/mock' }));
  apiButton(s, 'nav-awake', () => {
    setScreenAwakeMode({ enabled: true });
    return 'enabled';
  });
  apiButton(s, 'nav-secure', () => {
    setSecureScreen({ enabled: true });
    return 'enabled';
  });
  apiButton(s, 'nav-review', async () => {
    await requestReview();
    return undefined;
  });
  apiButton(s, 'nav-openurl', () => {
    openURL({ url: 'https://example.com' });
    return undefined;
  });
  apiButton(s, 'nav-swipe', () => {
    setIosSwipeGestureEnabled({ enabled: true });
    return undefined;
  });
  apiButton(s, 'nav-orientation', () => {
    setDeviceOrientation({ orientation: 'portrait' });
    return undefined;
  });
  apiButton(s, 'nav-share', () => {
    share({ text: 'hello' });
    return undefined;
  });
  apiButton(s, 'nav-close', () => {
    closeView();
    return undefined;
  });
}

// --- Environment ---
{
  const s = apiSection(app, 'environment', 'Environment');

  // Read-only values populated on load
  const platform = apiValue(s, 'env-platform', 'platform');
  const operational = apiValue(s, 'env-operational', 'operational');
  const network = apiValue(s, 'env-network', 'network');
  const appVersion = apiValue(s, 'env-app-version', 'appVersion');
  const locale = apiValue(s, 'env-locale', 'locale');
  const deviceId = apiValue(s, 'env-device-id', 'deviceId');
  const groupId = apiValue(s, 'env-group-id', 'groupId');
  const minVersion = apiValue(s, 'env-min-version', 'minVersionOk');
  const schemeUri = apiValue(s, 'env-scheme-uri', 'schemeUri');
  const safeTop = apiValue(s, 'env-safe-area-top', 'safeArea.top');

  async function refreshEnv() {
    platform.textContent = await Promise.resolve(getPlatformOS());
    operational.textContent = String(getOperationalEnvironment());
    network.textContent = await getNetworkStatus();
    appVersion.textContent = getTossAppVersion();
    locale.textContent = getLocale();
    deviceId.textContent = getDeviceId();
    groupId.textContent = getGroupId();
    minVersion.textContent = String(isMinVersionSupported({ minVersion: '1.0.0' }));
    schemeUri.textContent = getSchemeUri();
    // SafeAreaInsets is expected to be exposed as a function/object with `.top`.
    // If this call fails at typecheck, replace with whatever shape devtools mock exposes.
    const insets = (SafeAreaInsets as unknown as () => { top?: number })();
    safeTop.textContent = insets?.top != null ? String(insets.top) : 'n/a';
  }
  refreshEnv();

  // A button that re-reads getPlatformOS so Layer C (env OS bridge) can
  // observe changes after panel-driven state updates.
  apiButton(s, 'env-platform-refresh', async () => {
    const v = await Promise.resolve(getPlatformOS());
    platform.textContent = v;
    return v;
  });

  apiButton(s, 'env-server-time', async () => await getServerTime());
}

// --- Permissions ---
{
  const s = apiSection(app, 'permissions', 'Permissions');
  apiButton(s, 'perm-get', async () => await getPermission({ permissionName: 'camera' }));
  apiButton(s, 'perm-dialog', async () => await openPermissionDialog({ permissionName: 'camera' }));
  apiButton(s, 'perm-request', async () => await requestPermission({ permissionName: 'camera' }));
}

// --- Storage ---
{
  const s = apiSection(app, 'storage', 'Storage');
  apiInput(s, 'storage-key', 'key');
  apiInput(s, 'storage-value', 'value');
  apiButton(
    s,
    'storage-set',
    async ({ 'storage-key': k, 'storage-value': v }) => {
      await Storage.setItem({ key: k, value: v });
      return undefined;
    },
    { withInputs: ['storage-key', 'storage-value'] },
  );
  apiButton(
    s,
    'storage-get',
    async ({ 'storage-key': k }) => {
      const v = await Storage.getItem({ key: k });
      return v ?? '(null)';
    },
    { withInputs: ['storage-key'] },
  );
  apiButton(
    s,
    'storage-remove',
    async ({ 'storage-key': k }) => {
      await Storage.removeItem({ key: k });
      return undefined;
    },
    { withInputs: ['storage-key'] },
  );
  apiButton(s, 'storage-clear', async () => {
    await Storage.clearItems();
    return undefined;
  });
}

// --- Location ---
{
  const s = apiSection(app, 'location', 'Location');
  apiButton(s, 'location-current', async () => {
    const loc = await getCurrentLocation({ accuracy: 'high' });
    return loc;
  });
}

// --- Camera & Photos ---
{
  const s = apiSection(app, 'camera', 'Camera & Photos');
  apiButton(s, 'camera-open', async () => await openCamera());
  apiButton(s, 'photos-fetch', async () => {
    const photos = await fetchAlbumPhotos({ limit: 5 });
    return photos.length;
  });
}

// --- Contacts ---
{
  const s = apiSection(app, 'contacts', 'Contacts');
  apiButton(s, 'contacts-fetch', async () => {
    const list = await fetchContacts();
    return list;
  });
}

// --- Clipboard ---
{
  const s = apiSection(app, 'clipboard', 'Clipboard');
  apiInput(s, 'clipboard', 'text');
  apiButton(
    s,
    'clipboard-set',
    async ({ clipboard: v }) => {
      await setClipboardText({ text: v });
      return undefined;
    },
    { withInputs: ['clipboard'] },
  );
  apiButton(s, 'clipboard-get', async () => (await getClipboardText()) ?? '');
}

// --- Haptic ---
{
  const s = apiSection(app, 'haptic', 'Haptic');
  apiButton(s, 'haptic-tap', () => {
    generateHapticFeedback({ type: 'tap' });
    return undefined;
  });
  apiButton(s, 'save-base64', async () => {
    await saveBase64Data({ filename: 'x.png', base64: 'data:image/png;base64,iVBOR' });
    return undefined;
  });
}

// --- IAP ---
{
  const s = apiSection(app, 'iap', 'IAP');
  apiButton(s, 'iap-products', async () => {
    const list = await IAP.getProductItemList();
    return list;
  });
  apiButton(s, 'iap-purchase', async () => {
    const result = await IAP.createOneTimePurchaseOrder({ productId: 'mock-gem-100' });
    return `success:${JSON.stringify(result)}`;
  });
  apiButton(s, 'iap-sub', async () => {
    const result = await IAP.createSubscriptionPurchaseOrder({ productId: 'mock-sub' });
    return `success:${JSON.stringify(result)}`;
  });
  apiButton(s, 'iap-pending', async () => await IAP.getPendingOrders());
  apiButton(s, 'iap-completed', async () => await IAP.getCompletedOrRefundedOrders());
  apiButton(s, 'iap-subinfo', async () => await IAP.getSubscriptionInfo({ productId: 'mock-sub' }));
  apiButton(s, 'iap-checkout', async () => await checkoutPayment({ amount: 1000 }));
}

// --- Ads ---
{
  const s = apiSection(app, 'ads', 'Ads');
  apiButton(s, 'ads-admob-load', async () => {
    await GoogleAdMob.load({ unitId: 'mock-ad' });
    return 'loaded';
  });
  apiButton(s, 'ads-admob-show', async () => {
    await GoogleAdMob.show();
    return 'dismissed';
  });
  apiButton(s, 'ads-admob-isloaded', () => String(GoogleAdMob.isLoaded()));
  apiButton(s, 'ads-fullscreen-load', async () => {
    await loadFullScreenAd({ unitId: 'mock-full' });
    return 'loaded';
  });
  apiButton(s, 'ads-fullscreen-show', async () => {
    await showFullScreenAd();
    return 'dismissed';
  });
  apiButton(s, 'ads-tossads-init', async () => {
    await TossAds.initialize({ appId: 'mock-app' });
    return undefined;
  });
  apiButton(s, 'ads-tossads-destroy', () => {
    TossAds.destroyAll();
    return undefined;
  });
}

// --- Game ---
{
  const s = apiSection(app, 'game', 'Game');
  apiButton(s, 'game-promo', async () => await grantPromotionReward({ promotionId: 'mock' }));
  apiButton(s, 'game-promo-game', async () => await grantPromotionRewardForGame({ promotionId: 'mock' }));
  apiButton(s, 'game-score', async () => await submitGameCenterLeaderBoardScore({ boardId: 'mock', score: 100 }));
  apiButton(s, 'game-profile', async () => await getGameCenterGameProfile());
  apiButton(s, 'game-leaderboard', async () => {
    await openGameCenterLeaderboard({ boardId: 'mock' });
    return undefined;
  });
  apiButton(s, 'game-viral', async () => {
    await contactsViral({ message: 'mock' });
    return 'close';
  });
}

// --- Analytics ---
{
  const s = apiSection(app, 'analytics', 'Analytics');
  apiButton(s, 'analytics-click', () => {
    Analytics.click({ eventName: 'e2e-click' });
    return undefined;
  });
  apiButton(s, 'analytics-screen', () => {
    Analytics.screen({ screenName: 'e2e' });
    return undefined;
  });
  apiButton(s, 'analytics-impression', () => {
    Analytics.impression({ eventName: 'e2e-imp' });
    return undefined;
  });
  apiButton(s, 'analytics-eventlog', () => {
    eventLog({ eventName: 'e2e-log' });
    return undefined;
  });
}

// --- Partner ---
{
  const s = apiSection(app, 'partner', 'Partner');
  apiButton(s, 'partner-add', async () => {
    await partner.addAccessoryButton({ buttonId: 'mock' });
    return undefined;
  });
  apiButton(s, 'partner-remove', async () => {
    await partner.removeAccessoryButton({ buttonId: 'mock' });
    return undefined;
  });
}

// --- Events ---
{
  const s = apiSection(app, 'events', 'Events');
  apiSubscriber(s, 'events-back', (onEvent) => {
    graniteEvent.addEventListener('backEvent', { onEvent: () => onEvent(undefined) });
  });
  apiSubscriber(s, 'events-home', (onEvent) => {
    graniteEvent.addEventListener('homeEvent', { onEvent: () => onEvent(undefined) });
  });
  apiSubscriber(s, 'events-tds', (onEvent) => {
    tdsEvent.addEventListener('navigationAccessoryEvent', { onEvent: (e) => onEvent(e) });
  });
  apiSubscriber(s, 'events-visibility', (onEvent) => {
    onVisibilityChangedByTransparentServiceWeb({
      options: { callbackId: 'fixture-vis' },
      onEvent: (v) => onEvent({ isVisible: v }),
      onError: (e) => onEvent({ error: String(e) }),
    });
  });
}
```

**주의**:
- 각 SDK 호출 시그니처(예: `getTossShareLink({ path: '/mock' })`, `setScreenAwakeMode({ enabled: true })`)는 devtools mock의 실제 시그니처와 일치해야 한다. 불일치 시 typecheck에서 즉시 에러. 해당 SDK mock 파일(예: `src/mock/navigation/index.ts`)을 Read해 파라미터 이름/타입 확인 후 조정. **절대** 원본 파일을 수정하지 말고 호출부만 맞춘다.
- `SafeAreaInsets`의 실제 shape는 devtools mock의 `src/mock/navigation/*`에서 확인. 값/함수 어느 쪽이든 `insets?.top`을 안전하게 읽을 수 있게 호출부를 조정.

- [ ] **Step 2: typecheck**

Run:
```bash
cd /Users/dave/Projects/github.com/apps-in-toss-community/devtools-test-e2e-selector-audit
pnpm build                                   # devtools dist 필요 (unplugin resolveId 타깃)
pnpm exec tsc --noEmit -p e2e/fixture/tsconfig.json
```

Expected: PASS. 실패 유형별 조치:
- "Module @apps-in-toss/web-framework has no exported member 'X'" → devtools `src/mock/index.ts`에 해당 export가 있는지 확인. 없으면 main.ts에서 해당 import / 섹션 제거.
- 함수 시그니처 불일치 → devtools mock의 실제 시그니처로 호출 조정 (mock은 원본과 호환되도록 설계되어 있으므로 원본 SDK 시그니처 참고).

- [ ] **Step 3: fixture vite build**

Run:
```bash
pnpm exec vite build --config e2e/fixture/vite.config.ts
```

Expected: `e2e/fixture/dist/` 생성. `dist/index.html` + `dist/assets/*.js` 산출.

- [ ] **Step 4: fixture preview 육안 확인 (선택)**

Run:
```bash
pnpm exec vite preview --config e2e/fixture/vite.config.ts --port 4173 &
```

브라우저에서 `http://localhost:4173/` 열면 16개 섹션과 우측 하단의 AIT 토글 버튼이 보여야 한다. 확인 후 `kill %1`로 종료.

- [ ] **Step 5: 커밋**

Run:
```bash
git add e2e/fixture/main.ts
git commit -m "feat(e2e): fixture app renders all 16 domain sections"
```

---

## Task 6: `.gitignore` 보강

**Files:**
- Modify: `.gitignore`

- [ ] **Step 1: 추가 entry 삽입**

파일에 다음 줄을 추가 (이미 있으면 skip):
```
e2e/fixture/dist/
```

- [ ] **Step 2: 커밋**

Run:
```bash
git add .gitignore
git commit -m "chore(e2e): ignore fixture build output"
```

---

## Task 7: `e2e/panel.test.ts` 재작성 — 골조 + Smoke

**Files:**
- Rewrite: `e2e/panel.test.ts`

파일 전체를 교체한다.

- [ ] **Step 1: 새 내용으로 교체**

```ts
import { test, expect, type Page } from '@playwright/test';

// ============================================================================
// Helpers
// ============================================================================

async function gotoFixture(page: Page) {
  await page.goto('/');
  await expect(page.getByTestId('section-auth')).toBeVisible();
}

async function openPanel(page: Page) {
  await page.locator('button.ait-panel-toggle').click();
  await expect(page.locator('.ait-panel.open')).toBeVisible();
}

async function closePanel(page: Page) {
  await page.locator('button.ait-panel-toggle').click();
  await expect(page.locator('.ait-panel.open')).not.toBeVisible();
}

async function switchTab(page: Page, tabId: string) {
  await page.locator(`.ait-panel-tab[data-tab="${tabId}"]`).click();
}

async function clickApi(page: Page, id: string) {
  await page.getByTestId(`${id}-btn`).click();
}

function result(page: Page, id: string) {
  return page.getByTestId(`${id}-result`);
}

// ============================================================================
// Smoke
// ============================================================================

test.describe('Smoke', () => {
  test('fixture renders all 16 sections with no page errors', async ({ page }) => {
    const errors: string[] = [];
    page.on('pageerror', (e) => errors.push(e.message));

    await gotoFixture(page);

    for (const id of [
      'auth',
      'navigation',
      'environment',
      'permissions',
      'storage',
      'location',
      'camera',
      'contacts',
      'clipboard',
      'haptic',
      'iap',
      'ads',
      'game',
      'analytics',
      'partner',
      'events',
    ]) {
      await expect(page.getByTestId(`section-${id}`)).toBeVisible();
    }

    // Devtools panel toggle should be mounted automatically via unplugin panel injection.
    await expect(page.locator('button.ait-panel-toggle')).toBeVisible();

    expect(errors).toHaveLength(0);
  });
});

// (Layer A/B/C describes added in subsequent tasks.)
```

- [ ] **Step 2: Smoke만 실행**

Run:
```bash
pnpm test:e2e --reporter=list --grep "Smoke"
```

Expected: 1 passed. webServer 최초 기동 시 devtools build + fixture vite build로 수십 초 소요. 그 이후 `reuseExistingServer`로 재사용.

실패 유형:
- `section-auth` not found → `main.ts`의 import 또는 helper 호출이 에러로 중단된 것. 브라우저 devtools 콘솔 메시지 확인.
- panel toggle 미존재 → unplugin `panel: true`가 실제로 동작하지 않거나, 빌드 시 entry 감지 실패. `pnpm exec vite build --mode development` 또는 Task 2의 vite.config 재확인.

- [ ] **Step 3: 커밋**

Run:
```bash
git add e2e/panel.test.ts
git commit -m "test(e2e): rewrite panel.test.ts with fixture helpers and smoke test"
```

---

## Task 8: Layer A — 도메인 smoke 12개

**Files:**
- Modify: `e2e/panel.test.ts`

Smoke describe 바로 다음에 삽입. 12개 도메인(env/permissions/clipboard/events 제외) × 1 API.

- [ ] **Step 1: Layer A describe 추가**

`e2e/panel.test.ts` 끝에 다음을 append (Smoke describe 뒤에):

```ts
// ============================================================================
// Layer A — domain smoke
// ============================================================================

interface DomainSmoke {
  id: string;           // API testid base (without -btn)
  expectText?: string;  // Substring expected in result
  timeout?: number;
}

const DOMAIN_SMOKES: DomainSmoke[] = [
  { id: 'auth-login' },                                 // appLogin returns auth code
  { id: 'nav-sharelink', expectText: 'toss.im/share' }, // getTossShareLink
  { id: 'storage-clear' },                              // clearItems -> done
  { id: 'location-current', expectText: '37.' },        // default mock coords start with 37.
  { id: 'photos-fetch' },                               // fetchAlbumPhotos returns count
  { id: 'contacts-fetch', expectText: '홍길동' },        // mock contacts
  { id: 'haptic-tap' },                                 // tap haptic -> done
  { id: 'iap-products', expectText: 'mock-gem-100' },
  { id: 'ads-admob-load', expectText: 'loaded', timeout: 5000 },
  { id: 'game-profile', expectText: 'MockPlayer' },
  { id: 'analytics-click' },                            // Analytics.click -> done
  { id: 'partner-add' },                                // addAccessoryButton -> done
];

test.describe('Layer A — domain smoke', () => {
  for (const { id, expectText, timeout } of DOMAIN_SMOKES) {
    test(`${id} runs and result is populated`, async ({ page }) => {
      await gotoFixture(page);
      await clickApi(page, id);
      if (expectText) {
        await expect(result(page, id)).toContainText(expectText, { timeout });
      } else {
        await expect(result(page, id)).not.toBeEmpty({ timeout });
      }
    });
  }
});
```

**참고 — expectText 값은 devtools mock의 실제 반환값에 맞춰야 한다.** 실패 시:
- `toss.im/share` → `src/mock/navigation/*.ts`에서 `getTossShareLink` mock이 어떤 문자열 반환하는지 확인 후 수정.
- `홍길동` → `src/mock/device/contacts.ts` mock name 확인.
- `mock-gem-100` → `src/mock/iap/*` 기본 상품 id 확인.
- `MockPlayer` → `src/mock/game/*` 프로필 이름 확인.

**기대 값이 다르면 mock을 고치지 말고 테스트의 expectText를 고친다.**

- [ ] **Step 2: Layer A만 실행**

Run:
```bash
pnpm test:e2e --reporter=list --grep "Layer A"
```

Expected: 12 passed. Flaky 하나 있으면 해당 테스트에 `timeout: 5000` 추가.

- [ ] **Step 3: 커밋**

Run:
```bash
git add e2e/panel.test.ts
git commit -m "test(e2e): add Layer A domain smoke (12 domains)"
```

---

## Task 9: Layer B — 패널 UX 4개

**Files:**
- Modify: `e2e/panel.test.ts`

Layer A 다음에 삽입.

- [ ] **Step 1: Layer B describe 추가**

```ts
// ============================================================================
// Layer B — panel UX
// ============================================================================

test.describe('Layer B — panel UX', () => {
  test('toggle opens and closes the panel', async ({ page }) => {
    await gotoFixture(page);
    const toggle = page.locator('button.ait-panel-toggle');
    await expect(toggle).toBeVisible();
    await expect(toggle).toHaveText('AIT');

    await openPanel(page);
    await closePanel(page);
  });

  test('dragging changes the toggle Y position', async ({ page }) => {
    await gotoFixture(page);
    const toggle = page.locator('button.ait-panel-toggle');
    const box = await toggle.boundingBox();
    expect(box).not.toBeNull();
    const startX = box!.x + box!.width / 2;
    const startY = box!.y + box!.height / 2;

    await page.mouse.move(startX, startY);
    await page.mouse.down();
    await page.mouse.move(startX - 100, startY - 80, { steps: 10 });
    await page.mouse.up();

    const newBox = await toggle.boundingBox();
    expect(newBox).not.toBeNull();
    expect(Math.abs(newBox!.y - box!.y)).toBeGreaterThan(10);
  });

  test.describe('mobile fullscreen', () => {
    test.use({ viewport: { width: 375, height: 667 } });

    test('panel opens fullscreen and close button hides it', async ({ page }) => {
      await gotoFixture(page);
      await openPanel(page);

      const panel = page.locator('.ait-panel.open');
      const box = await panel.boundingBox();
      expect(box).not.toBeNull();
      const viewport = page.viewportSize()!;
      expect(box!.x).toBe(0);
      expect(box!.y).toBe(0);
      expect(box!.width).toBe(viewport.width);
      expect(box!.height).toBe(viewport.height);

      const closeBtn = page.locator('.ait-panel-close');
      await expect(closeBtn).toBeVisible();
      await closeBtn.click();
      await expect(page.locator('.ait-panel.open')).not.toBeVisible();
    });
  });

  test('toggle button position persists across reload', async ({ page }) => {
    await gotoFixture(page);
    const toggle = page.locator('button.ait-panel-toggle');
    const box = await toggle.boundingBox();
    expect(box).not.toBeNull();

    const startX = box!.x + box!.width / 2;
    const startY = box!.y + box!.height / 2;

    await page.mouse.move(startX, startY);
    await page.mouse.down();
    await page.mouse.move(80, 300, { steps: 10 });
    await page.mouse.up();

    const draggedBox = await toggle.boundingBox();
    expect(draggedBox).not.toBeNull();

    const saved = await page.evaluate(() => localStorage.getItem('__ait_btn_pos'));
    expect(saved).not.toBeNull();
    const pos = JSON.parse(saved!) as Record<string, string>;
    expect(pos).toHaveProperty('left', '16px');

    await page.reload();
    await expect(page.getByTestId('section-auth')).toBeVisible();

    const restored = page.locator('button.ait-panel-toggle');
    await expect(restored).toBeVisible();
    const restoredBox = await restored.boundingBox();
    expect(restoredBox).not.toBeNull();
    expect(restoredBox!.x).toBe(16);
    expect(Math.abs(restoredBox!.y - draggedBox!.y)).toBeLessThan(5);
  });
});
```

- [ ] **Step 2: Layer B만 실행**

Run:
```bash
pnpm test:e2e --reporter=list --grep "Layer B"
```

Expected: 4 passed.

- [ ] **Step 3: 커밋**

Run:
```bash
git add e2e/panel.test.ts
git commit -m "test(e2e): add Layer B panel UX (toggle, drag, fullscreen, persistence)"
```

---

## Task 10: Layer C — 패널 ↔ 앱 bridge 4개

**Files:**
- Modify: `e2e/panel.test.ts`

Layer B 다음에 삽입.

- [ ] **Step 1: Layer C describe 추가**

```ts
// ============================================================================
// Layer C — panel ↔ app bridge
// ============================================================================

test.describe('Layer C — bridge', () => {
  test('env: panel OS change reflects in getPlatformOS', async ({ page }) => {
    await gotoFixture(page);

    await openPanel(page);
    await switchTab(page, 'env');
    const osRow = page
      .locator('.ait-panel .ait-row')
      .filter({ has: page.locator('label', { hasText: /^OS$/ }) });
    await osRow.locator('select').selectOption('android');
    await closePanel(page);

    await clickApi(page, 'env-platform-refresh');
    await expect(result(page, 'env-platform-refresh')).toHaveText('android', { timeout: 5000 });
    await expect(page.getByTestId('env-platform-value')).toHaveText('android');
  });

  test('permissions: denied camera causes openCamera error', async ({ page }) => {
    await gotoFixture(page);

    await openPanel(page);
    await switchTab(page, 'permissions');
    const cameraSelect = page
      .locator('.ait-panel .ait-row')
      .filter({ hasText: 'camera' })
      .locator('select')
      .first();
    await cameraSelect.selectOption('denied');
    await closePanel(page);

    await clickApi(page, 'camera-open');
    await expect(result(page, 'camera-open')).toContainText('error:', { timeout: 5000 });
    await expect(result(page, 'camera-open')).toContainText('denied');
  });

  test('device: clipboard mock mode round-trips text', async ({ page }) => {
    await gotoFixture(page);

    await openPanel(page);
    await switchTab(page, 'device');
    const clipboardSelect = page
      .locator('.ait-panel .ait-row')
      .filter({ hasText: 'Clipboard' })
      .locator('select');
    await clipboardSelect.selectOption('mock');
    await closePanel(page);

    await page.getByTestId('clipboard-input').fill('hello-bridge');
    await clickApi(page, 'clipboard-set');
    await expect(result(page, 'clipboard-set')).toHaveText('done');

    await clickApi(page, 'clipboard-get');
    await expect(result(page, 'clipboard-get')).toHaveText('hello-bridge');
  });

  test('events: panel trigger back event is received by subscriber', async ({ page }) => {
    await gotoFixture(page);

    // Subscribe first
    await clickApi(page, 'events-back');
    await expect(page.getByTestId('events-back-empty')).toBeVisible();

    // Trigger from panel
    await openPanel(page);
    await switchTab(page, 'events');
    await page.locator('.ait-panel button').filter({ hasText: 'Trigger Back Event' }).click();
    await closePanel(page);

    // Empty sentinel should disappear and a log entry should appear.
    await expect(page.getByTestId('events-back-empty')).not.toBeVisible({ timeout: 3000 });
    await expect(page.getByTestId('events-back-log').locator('div').first()).toBeVisible();
  });
});
```

- [ ] **Step 2: Layer C만 실행**

Run:
```bash
pnpm test:e2e --reporter=list --grep "Layer C"
```

Expected: 4 passed. 실패 대응:
- `getPlatformOS` 결과가 `android` 아님 → devtools panel env 탭 select의 option value 이름 확인 (`src/panel/tabs/environment.ts`).
- `camera-open` 결과가 Success → `src/mock/device/camera.ts`와 `withPermission` 로직 상 permission denied 시 throw 되는지 확인. 동작은 실제로 맞는데 테스트 selector 문제일 수 있으므로 `result-${id}` 텍스트를 `page.evaluate`로 찍어 본다.
- Clipboard round-trip 실패 → `src/mock/device/clipboard.ts`의 `mock` 모드가 실제로 메모리에 값을 저장하는지 확인. 저장 안 하면 devtools 이슈이지만 **이 PR에서 수정하지 않음** — 해당 테스트 `test.skip`과 커밋 메시지에 기록.
- Back event log 미증가 → `src/panel/tabs/events.ts`의 트리거 버튼 텍스트가 정말 "Trigger Back Event"인지 확인.

- [ ] **Step 3: 커밋**

Run:
```bash
git add e2e/panel.test.ts
git commit -m "test(e2e): add Layer C panel-app bridge (env, permissions, device, events)"
```

---

## Task 11: 전체 green + 안정성 + 구 문서 정리 + PR

**Files:**
- Delete: `docs/superpowers/specs/2026-04-18-e2e-testid-contract-design.md`
- Delete: `docs/superpowers/plans/2026-04-18-A-e2e-selector-audit.md`
- Delete: `docs/superpowers/plans/2026-04-18-e2e-testid-contract.md`

- [ ] **Step 1: 전체 스위트 실행**

Run:
```bash
pnpm test:e2e --reporter=list 2>&1 | tee /tmp/e2e-final.log
```

Expected: 21 passed, 0 failed.

- [ ] **Step 2: 안정성 — 2회 연속**

Run:
```bash
pnpm test:e2e --reporter=list && pnpm test:e2e --reporter=list
```

Expected: 두 번 연속 21 passed.

- [ ] **Step 3: 구 문서 삭제**

Run:
```bash
git rm docs/superpowers/specs/2026-04-18-e2e-testid-contract-design.md \
       docs/superpowers/plans/2026-04-18-A-e2e-selector-audit.md \
       docs/superpowers/plans/2026-04-18-e2e-testid-contract.md
```

Expected: 3개 파일 삭제 stage.

- [ ] **Step 4: 삭제 커밋**

Run:
```bash
git commit -m "docs: remove superseded e2e plans and testid-contract spec"
```

- [ ] **Step 5: push**

Run:
```bash
git push -u origin test-e2e-selector-audit
```

- [ ] **Step 6: PR 생성**

Run:
```bash
gh pr create --title "test(e2e): self-contained fixture replaces sdk-example clone" --body "$(cat <<'EOF'
## Summary

devtools의 Playwright E2E를 자기완결(in-repo fixture)로 만든다. `playwright.config.ts`가 더 이상 `sdk-example`을 git clone / install / build하지 않는다. 대신 `e2e/fixture/`에 Vite + `@ait-co/devtools/unplugin`으로 돌아가는 최소 consumer 앱을 내장하고, 테스트 21개로 재작성:

- **Smoke (1)** — 16개 섹션 렌더 + panel 자동 마운트 + pageerror 0
- **Layer A 도메인 smoke (12)** — 각 도메인 대표 API 1개 실행 → result 채워짐
- **Layer B 패널 UX (4)** — toggle / drag / mobile fullscreen / position persistence
- **Layer C 패널 ↔ 앱 bridge (4)** — env OS 변경 / camera permission denied / clipboard mock 왕복 / events Trigger Back

## 왜 이 방향

원래 이 브랜치의 계획은 sdk-example에 testid 계약을 부여해 consumer로 사용하는 것이었지만, cross-repo 결합이 과하다는 판단. devtools의 핵심 가치인 unplugin 경로와 panel 마운트는 fixture로도 동일하게 검증되며, sdk-example은 외부 사용자 시나리오로서 독립된 dog-fooding 트랙으로 분리. 설계: `docs/superpowers/specs/2026-04-18-e2e-fixture-design.md`.

## Scope

- `e2e/fixture/` 신규 (index.html, main.ts, helpers.ts, vite.config.ts, tsconfig.json)
- `playwright.config.ts` webServer 교체
- `e2e/panel.test.ts` 전면 재작성 (848 → ~420 lines)
- `package.json`에 `vite` devDep 추가
- 폐기된 spec/plan 문서 3개 삭제

## Test plan

- [x] 로컬 `pnpm test:e2e` 2회 연속 21 passed (`/tmp/e2e-final.log` 참고)
- [ ] CI `build-and-test` job green (기존 유닛만, E2E는 PR B로 게이팅)
EOF
)"
```

Expected: PR URL 출력.

---

## Self-review 체크리스트

- [ ] `e2e/fixture/`, `playwright.config.ts`, `e2e/panel.test.ts`, `package.json`, `.gitignore` 외 수정 없음
- [ ] devtools `src/` 건드리지 않음
- [ ] sdk-example 건드리지 않음
- [ ] `.tmp/sdk-example` 관련 참조 제거 (playwright.config.ts의 clone 명령)
- [ ] `pnpm test:e2e` 2회 연속 green
- [ ] 구 spec/plan 문서 3개 삭제됨
- [ ] unplugin `panel: true`가 실제로 동작해 AIT 토글 버튼이 fixture에 뜸
