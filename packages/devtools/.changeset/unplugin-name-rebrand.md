---
'@apps-in-toss/devtools': patch
---

unplugin 플러그인 `name`을 `ait-co-devtools`에서 `apps-in-toss-devtools`로 개명한다. Vite/Rollup 플러그인 목록과 에러 프리픽스(`[plugin apps-in-toss-devtools] …`)에 노출되는 표시 문자열이라 이름으로 플러그인을 찾던 설정이 있다면 새 이름을 쓰면 된다 — resolve 경로·동작 자체는 바뀌지 않는다.
