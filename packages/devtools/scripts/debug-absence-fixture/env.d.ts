// Consumer-build constant injected by this fixture's vite.config.ts `define`
// (#647). Fixture-scoped: devtools `src/` must never reference __DEBUG_BUILD__
// bare, so it is deliberately absent from src/env.d.ts.
declare const __DEBUG_BUILD__: boolean;
