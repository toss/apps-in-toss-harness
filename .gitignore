node_modules/
dist/
build/
coverage/
.DS_Store

# Claude Code
.claude/settings.local.json
.claude/*.lock
.claude.lock

# maintainer-local 운영 문서 — repo 미포함(2026-08-27 내부 운영 표면 정리)
# 루트 앵커(`/docs/`)는 의도다 — 앵커 없는 `docs/`는 하위 경로의 동명 디렉터리까지
# 삼켜서, 배포 산출물인 templates/react-vite/docs/design-guide.md 가 커밋에서
# 조용히 빠졌다.
/docs/

# maintainer-local 세션 skill — repo 미포함(2026-08-27 내부 운영 표면 정리)
.claude/skills/

# maintainer-local 측정 인프라 — repo 미포함(2026-08-27 repo 다이어트)
packages/agent-plugin/eval/

# SECRET-HANDLING (devtools/debugger relay): relay state files carry TOTP
# secrets, wss:// relay URLs, and tunnel hostnames. Never track them.
.ait_relay
.ait_urls

# SECRET-HANDLING (eval 슈트 B): ANTHROPIC_API_KEY 주입 파일. op:// 참조가
# 권장이지만 op 미설치 환경에선 평문 키가 들어올 수 있다 — 추적 금지.
.env.eval

# Cursor — 플러그인 활성화 기록(로컬 상태, 머신·사용자별)
/.cursor/settings.json
